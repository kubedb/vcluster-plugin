apiVersion: kubedb.com/v1alpha2
kind: Postgres
metadata:
  name: test-pg
  namespace: demo
spec:
  version: "13.13"
  storageType: Durable
  storage:
    accessModes:
    - ReadWriteOnce
    resources:
      requests:
        storage: 1Gi
  terminationPolicy: WipeOut